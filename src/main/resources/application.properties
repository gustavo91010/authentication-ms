server.port=${PORTS}
app.info.enviroment=${ENVIROMENT}
# senha do usuairo padrão: ajudaqui / @Ajudaqui
app.auth.master=77a1e032-823a-4793-ac96-2e34567b3059

# Database
spring.data.mongodb.uri=${MONGO_URI}
spring.data.mongodb.auto-index-creation=true

# App Properties
bezkoder.app.jwtSecret= ajudaqui-ms
bezkoder.app.jwtExpirationMs= 86400000

# Trabalha jundo com o handle exception na exceção:NoHandlerFoundException
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=true

# Configura00e7ão para buscar dados no Config-Server-API
# spring.application.name=authentication-ms
# spring.cloud.config.uri=http://localhost:8888
# spring.profiles.active=default

management.endpoints.web.exposure.include=health,info,metrics,env
management.endpoint.health.show-details=always

# Thymeleaf
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.cache=false
# Configura00e7ão aws
# Preciso garantir que as variaves aws_access_key_id e aws_secret_access_key estejam no .env
aws.region=${AWS_REGION}
aws.id=${AWS_ID}

logging.level.org.springframework.boot.context.properties=DEBUG
aws.fila.bill-manager=lalala
aws.fila.gestor360=lelele
aws.fila.names=bill-manager-register-a5979793-2ccd-4adf-b1f8-276e104eb954,gestor360-98362493765,auth-193846273161

# as.fila.bill-manager=${AWS_FILA_BILL_MANAGER}
# aws.fila.gestor360=${AWS_FILA_GESTOR360}

oauth.github.redirect-uri=${GITHUB_REDIRECT_URI}
oauth.github.client-id=${GITHUB_CLIENT_ID}
oauth.github.client-secret=${GITHUB_CLIENT_SECRET}

oauth.google.redirect-uri=${GOOGLE_REDIRECT_URI}
oauth.google.client-id=${GOOGLE_CLIENT_ID}
oauth.google.client-secret=${GOOGLE_CLIENT_SECRET}


info.app.environment=${ENVIROMENT}
info.app.description=Sistema de autentica\u00e7\u00e3o para m\u00faltiplas aplica\u00e7\u00f5es
info.app.url=${APP_URL}
info.app.name=${APP_NAME}
info.app.version=${VERSION}
info.app.contact=${CONTACT}



spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME}
spring.mail.password=${EMAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
